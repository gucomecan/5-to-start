import React from 'react';
import { View, Text, TextInput, StyleSheet } from 'react-native'

import { Item } from './types'

const Item:React.FC<Item> = ({name, date, amount}) => {
  return (
    <View>
      
    </View>
  );
}

export default Item;
